'use strict';

var program = require('commander');
var {Config} = require('ripple-command/config');
var {Console} = require('ripple-command/console');
var {RippleWrapper} = require('ripple-command/api/ripple-wrapper');

program
    .command('get-server-info')
    .description('Returns info on the configured ripple server.')
    .action(function(cmd, options) {
        Config.absorb(program.opts(), cmd.opts());
        (new RippleWrapper()).invoke('getServerInfo').then((info) => {
            Console.out('Server:');
            Console.out(info, 1);
        });
    })
    ;
