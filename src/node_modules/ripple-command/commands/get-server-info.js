'use strict';

const program = require('commander');
const {Command} = require('ripple-command/command');

program
    .command('get-server-info')

    .description('Returns info on the configured ripple server.')

    .action(Command.actionHandler({
        apiCall: 'getServerInfo',
    }))
;
