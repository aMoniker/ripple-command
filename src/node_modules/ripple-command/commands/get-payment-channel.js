'use strict';

const program = require('commander');
const {Command} = require('ripple-command/command');

program
    .command('get-payment-channel <id>')
    .description('Returns specified payment channel. <id> is a 256-bit hexadecimal channel identifier.')
    .action(Command.actionHandler({
        apiCall: 'getPaymentChannel',
    }))
    ;
